@model ShoppingCart.Data.Entity.Order

<div class="card margin border-color-strip" style="width: 18rem;">
    <div class="card-body">
        <label class="card-title font-weight-bold">Ref No: @Model.Id</label>
        @{ if (@Model.Status == 0)
            {
                <span class="badge badge-warning right">Status: @Model.Status</span>
            }
            else
            {
                <span class="badge badge-success right">Status: @Model.Status</span>
            }
        }
        <hr />
        <label class="small">Customer Name:</label>
        <p class="card-text font-weight-bold">@Model.Customers.FirstName @Model.Customers.LastName</p>
        <label class="small">Date:</label>
        <p class="card-text font-weight-bold">@Model.Date</p>

        <hr />
        <div class="btn-group" role="group" aria-label="Basic example">
            <a class="btn btn-info btn-sm" asp-controller="Order" asp-action="OrderDetail" asp-route-id="@Model.Id">More</a>
            @{ if (@Model.Status == 0)
                {
                    <a class="btn btn-warning btn-sm" asp-controller="Order" asp-action="OrderDetail" asp-route-id="@Model.Id">Edit</a>
                }
            }
        <form method="post" asp-controller="Order" asp-action="OrderDelete" asp-route-id="@Model.Id">
            <input type="submit" class="btn btn-danger btn-sm" value="Remove" />
        </form>
        </div>
    </div>
</div>

@*<div class="modal fade" id="confirmationBox" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title font-weight-bold" id="exampleModalLongTitle">Order Remove Confirmation</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                Are you sure you want remove the order?
            </div>
            <div class="modal-footer">
                
                <button type="button" class="btn btn-secondary" data-dismiss="modal">No</button>
            </div>
        </div>
    </div>
</div>*@